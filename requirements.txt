# Requirements for PLUMATOTM Astrological Animal Compatibility Engine
# Use with Python 3.11 for pre-compiled wheels

# Core astrological library
pyswisseph==2.08.00-1
flatlib>=0.2.3

# Data processing
pandas>=2.3.0
numpy>=2.3.0

# Date and time handling
python-dateutil>=2.9.0
pytz>=2025.0

# Geocoding (optional - for place name to coordinates conversion)
geopy>=2.4.0

# Timezone detection (for local to UTC conversion)
timezonefinder>=6.5.0

# Visualization (for radar charts)
matplotlib>=3.10.0
